# ls
# --------------------------------------------------------------------
alias ll='ls -lh'
alias la='ls -A'
alias l='ls -CF'

# Misc.
# --------------------------------------------------------------------
alias c='clear'
