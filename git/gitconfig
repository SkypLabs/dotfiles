[user]
	name = Paul-Emmanuel Raoul
	email = skyper@skyplabs.net
	signingkey = AA73D4B7CDB8C0546B47F061063BB9E8C569B26A
[alias]
	co = checkout
	dw = diff --word-diff
	fa = fetch --all --tags --prune
	ld = log --stat -p
	lg = log --decorate --all --oneline --graph
	sd = diff --staged
	sdw = diff --staged --word-diff
	ss = status
[init]
	defaultBranch = main
[core]
	editor = vim
	autocrlf = input
[merge]
	tool = vimdiff
[diff]
	tool = vimdiff
[difftool]
	prompt = false
[pull]
	ff = only
[gpg]
	program = qubes-gpg-client-wrapper
[gitflow "prefix"]
	feature = feature/
	bugfix = bugfix/
	release = release/
	hotfix = hotfix/
	support = support/
	versiontag = v
