- defaults:
    link:
      relink: true
      force: true

- clean: ['~']

- link:
    ~/.bashrc:
    ~/.bash_aliases:
    ~/.gitconfig:
    ~/.inputrc:
    ~/.tmux.conf:

- shell:
  - [git submodule update --init --recursive, Installing Git submodules]
